// Your test code here, it is for custom elements

// for example:

/* describe('Given a summary component', () => {
  const data = mockData;
  const domContainer = document.getElementById('dom');
  let element;
  const expect = chai.expect;

  beforeEach((done) => {
    element = document.createElement('md-summary');
    element.classList.add('alert', 'alert-secondary');
    domContainer.appendChild(element);
    done();
  });

  describe('When the developer wants to shows data in the summary', () => {
    
    it('should show the heading', (done) => {
      element.heading = data.heading;
      expect(element.shadowRoot.textContent).to.contain(data.heading);
      done();
    });

  });

}); */
